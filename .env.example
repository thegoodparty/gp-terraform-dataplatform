# =============================================================================
# Local Development Environment Variables
# =============================================================================
# IMPORTANT: Copy this to .env and fill in real values. Never commit .env!
#
# Usage: source .env && terraform plan

# Astronomer API Token (required for local development)
# Note: `astro login` does NOT work - the Terraform provider requires this env var
# Create token in Astro: Organization Settings > Access Management > API Tokens
# Required permissions: Organization Owner (to create deployments)
ASTRO_API_TOKEN=your-astro-api-token
